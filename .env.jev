# Jev test build mode — enables the AI assistant UI layer (src/ai, AssistantBar,
# Settings → AI section). The main `npm run build` never loads this file, so the
# production bundle contains no AI code paths.
# Jev 测试构建模式——启用 AI 助手层（src/ai、AssistantBar、设置→AI 分节）。
# 主线 `npm run build` 不加载此文件，正式产物不含任何 AI 代码路径。
VITE_JEV_BUILD=1
